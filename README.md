# Gradient Atlas

ML Materials

Static site. Run locally from this folder:

    python3 -m http.server 8000

then open http://localhost:8000

Opening index.html by double-click (file://) will not work, because the pages load their chapter data with fetch().
